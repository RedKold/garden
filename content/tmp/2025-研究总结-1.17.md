## 研究主题
金融风险指标 MES 和 SRISK 的 Monte Carlo 算法
当前主要着眼于 MES

*MES* 定义

将 $R$ 拆分为 $\sum_{i}y_{i}r_{i}$, 
	where $y_i$ is the **weight** of group $i$ in the total portfolio (投资组合)
那么
$$
ES_{\alpha}=-\sum\limits_{i}y_{i}E[r_{i}|R\leq-VaR_{\alpha}] 
$$
对 $y_i$ 求偏导
$$
\frac{\partial ES_{\alpha}}{\partial y_{i}}=-E[r_{i}|R\leq-VaR_{\alpha}] \equiv MES^i_{\alpha}
$$
MES 的数学结构是一个条件期望的形式。

难点攻关：
- 如何处理 $R\leq -VaR_{\alpha}$ 的「小于」条件?
	- 用贝叶斯公式，拆解条件期望为两个期望, 的比值。
	- boolean mask
$$
\widehat{\text{MES}} = -\frac{\sum_i r_i \, \mathbf{1}(r_m \leq -VaR) \, w_{\theta^*}(x_i)}
{\sum_i \mathbf{1}(r_m \leq -VaR) \, w_{\theta^*}(x_i)}.
$$
	- here we use a boolean mask variable $\mathbf{1}(r_{m}\leq -VaR)$, to select conditional *data*  from sample
		- soft indicator
	- $w_{\theta^{*}}$ : 重要性权重
		- $w_i = \frac{p(x_i)}{q_{\theta_t}(x_i)}.$
		- $\theta^{*}$ 是使用 CE 得到了最优的提议分布的参数下的 $w_{\theta^{*}}$

- 在估计时候，涉及 $VaR_{\alpha}$ 和 $MES$ 的两阶段估计问题，需要分阶段考虑误差和收敛效率。在用样本进行估计的时候，分配不同阶段的样本数也涉及一个 trade off


### 1. Problem Definition

We aim to estimate the **Marginal Expected Shortfall (MES)**:

$$
\text{MES} = -\mathbb{E}[r_i \mid r_m \leq -\text{VaR}_\alpha]
$$

More generally, we consider the expectation of a function under some distribution $p(x)$:

$$
\theta^* = \mathbb{E}_p[h(X)] = \int h(x)p(x)\,dx
$$

However, when $h(x)$ depends on **rare events** (e.g. $r_m \leq -\text{VaR}$), direct Monte Carlo estimation is inefficient because most samples contribute nothing.

---

### 2. Importance Sampling (IS)

We rewrite the expectation under a proposal distribution $q_\theta(x)$:

$$
\mathbb{E}_p[h(X)] = \int h(x)\frac{p(x)}{q_\theta(x)} q_\theta(x)\,dx = \mathbb{E}_{q_\theta}[h(X) w_\theta(X)]
$$

where the **importance weight** is

$$
w_\theta(x) = \frac{p(x)}{q_\theta(x)}.
$$

A good choice of $q_\theta$ increases the **probability of sampling** from the rare event region, reducing estimator variance.



## CE 方法回顾
CE 方法即 Cross Entropy
[[CE - 新的启发-1129]]

![image.png|500](https://kold.oss-cn-shanghai.aliyuncs.com/20251128202514.png)

- 文章*A Cross-Entropy Method for Value-at-Risk Constrained Optimization* 研究在 Value-at-Risk 作为约束的情况下的优化问题，实际上我们如果使用 CE 方法计算 MES，实际也是在 $VaR$ 的特定约束下做的，可以借鉴方法。

- 待攻关的难点
	- 如何证明收敛性？
	- 如何证明收敛速度？
- 这篇文章也没有给出证明。直观上感受，证明 CE 方法的 convergence properties 很困难
	- ![image.png|400](https://kold.oss-cn-shanghai.aliyuncs.com/20260117120011.png)

- *Convergence properties of the cross-entropy method for discrete optimization*，
- https://people.smp.uq.edu.au/DirkKroese/ps/CEconv.pdf
- 一篇值得研究的文章