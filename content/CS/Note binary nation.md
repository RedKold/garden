- 二进制国度

这是一个背包问题。（**动态规划经典模型**）

用 `dp[i]` 表达换 `i` 元钱可以的方式。

显然 `dp[1]=1`, 规定 `dp[0]=0`

我们满足的约束：总共要凑 `n` 元钱。

求 `dp[n]`

在选定一种金额 `coin` 时，剩下的金额凑出的方案是 `dp[n-coin]`

同时我们需要遍历所有 `coins`。

为了不重复计算，我们从 `coins` 的较小值开始计算，相当于每次用一种钱币凑 `dp[n]`，下一次迭代就不再用这个 `coin`, 这样可以避免重复的问题。

```python
# python语法，你可以看成伪代码
for coin in coins:
    for i in range(coin, n+1):
        dp[i] += dp[i - coin]
```
