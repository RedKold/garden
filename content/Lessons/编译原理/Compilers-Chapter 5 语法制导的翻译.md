# 第五章-语法制导的翻译
- 使用上下文无关文法（CFG）引导语言的翻译
	- **CFG**的非终结符号代表了语言的某个构造
	- 程序设计语言的构造由更小的构造组合而成
- **一个构造的语义的来源**：
	- 由小构造的含义 **综合** 而来
		- 比如表达式 $x+y$ 的类型由 $x,y$ 的类型和运算符 $+$ 决定
		- 也可以从附近的构造继承而来
			- **比如**：声明 int x 中 x 的类型由它左边的类型表达式决定

## 语法制导
> [!Definition] 语法制导定义
> 将文法符号和某些属性相关联，并通过 **语义规则** 来描述如何计算属性的值
> - $E\to E_{1}+T$, $E.code=E_{1}.code\;||\;T.code\;||\;'+'$ 
> - 属性 $code$ 代表表达式的逆波兰表示。规则说明加法表达式的逆波兰表示由两个分量的逆波兰表示并置，然后加上 `+` 得到（中缀表达式->后缀表达式）

> [!Note] 语法制导翻译
> - 在产生式体中加入 **语义动作**，并在适当时候执行动作
> 	- $E\to E_{1}+T$  {printf `+`;}


### 语法制导的定义
- **Syntax-Directed Definition**（SDD）是上下文无关文法和属性/规则的结合
	- 属性和文法符号相关联，按照需要来确定各个文法符号需要哪些属性
	- 规则和产生式相关联
- 对于文法符号 $X$ 和属性 $a$，我们用 $X.a$ 表示分析树中某个标号为 $X$ 的结点的值
	- 一个分析树结点和它的分支对应一个产生式规则，而对应的语义规则确定了这些结点上属性的取值和计算
 - **事实上**，在 bison 编程实践中，我们使用的就是 SDD。

### 综合属性和继承属性
考虑*分析树*结点 $N$ 上的非终结符号 $A$ 的 2 种属性
- 综合属性 (synthesized attribute)
	- 结点 $N$ 的属性值由 $N$ 的产生式所关联的语义规则来定义
	- 通过 $N$ 的**子结点**或 $N$ **本身**的属性值来定义
- 继承属性（inherited attribute）
	- 结点 $N$ 的属性值由 $N$ 的父结点所关联的语义规则来定义
	- 依赖于 $N$ 的父结点，$N$ 本身和 $n$ 的兄弟结点上的属性值
- 几条约束
	- **不允许**$N$ 的继承属性通过 $N$ 的子结点上的属性值来定义，但是我们允许结点 $N$ 上的一个综合属性通过结点 $N$ 本身的继承属性来定义
	- *终结符号*有综合属性（来自词法分析），但无继承属性
		- 符号表已经保存了，不要再继承了，是最原始的了

## 分析树和属性值
- **假设我们需要知道**一个表达式的类型，以及对应代码将它的值存放的内存地址
	- **两个属性**：*type, place*
- 把属性值一个个都存上去

## SDD 的例子
![image.png|400](https://kold.oss-cn-shanghai.aliyuncs.com/20260401104530.png)

## S 属性的 SDD
- **只包含**综合属性的 SDD 称为 S 属性的 SDD
	- 每个语义规则都根据产生式体中的属性值来计算头部非终结符号的属性值
- S 属性的 SDD 可以和 LR 语法分析器一起实现
	- 栈中的状态/文法符号可以附加相应的属性值
	- **归约时**，按照语义规则计算规约得到的符号的属性值
- 语义规则不应该有复杂的 *副作用*
	- *很微妙*，复杂的？副作用？
	- 要求副作用不影响其他属性的求值
	- 没有副作用的 SDD 称为属性文法 (attribute grammar)


## 语法分析树上的 SDD 求值

- 在分析树上求值有助于翻译方案的可视化，方便理解。
### 注释语法分析树
> [!Note] **注释**语法分析树 (annotated parse tree)
>  -  包含了各个结点的各*属性值*的语法分析树


- **步骤**
	- 对于任意的输入串，先构造出相应的分析树
	- 给各个结点（根据文法符号）加上相应的属性
	- 按照语义规则计算这些属性的值

- 按照分析树上的分支对应的文法产生式，应用相应的语义规则计算属性值

- **计算顺序**
	- 如果某个结点 $N$ 的属性 $a$ 为 $f(N_{1}\cdot b_{1},N_{2}.b_{2},\dots,N_{k}.b_{k})$, 那么我们需要先算出 $N_{1}\cdot b_{1},N_{2}.b_{2},\dots,N_{k}.b_{k}$ 的值

- 如果可以给各个属性值排出 *计算顺序*，那么这个注释分析树就可以计算得到
	- 一个*偏序关系*
	- S 属性的 SDD 一定可以按照 *自底向上* 的方式求值
- 下面的 SDD 不能计算（循环定义）
	- $A\to B,\;A.s=B.i,\;B.i=A.s+1$
	- 出现了左递归。

- ![image.png|400](https://kold.oss-cn-shanghai.aliyuncs.com/20260401112442.png)

## 适用于自顶向下的 SDD (1)
> [!Note] 分果果游戏
> chang xu 老师进行了分果果游戏，把卡片分给不同的同学，第一轮让他们和自己各说一句话（不可彼此交流），来统计果果数量。（综合）
> 第二轮让同学们只能和左边和右边的同学说话（简单的继承）
> 第三轮把果果变成了*算子*



> [!Question] **继承属性**的使用动机
> 文法存在递归，无法使用自顶向下的分析方法处理
> - **消除左递归**
> 	- 消除左递归改变了语法树结构！
> 	- 语法树结构和抽象语法不匹配！
> - **使用继承属性**
> - compare to $T\to T*F\;|\;F$
> 	- $T\to F\;T',\; T'\to*F\;T'\;|\;\varepsilon$
> - $T$ 对应的项中，第一个因子对应 $F$，而运算符却在 $T'$ 中

![image.png|400](https://kold.oss-cn-shanghai.aliyuncs.com/20260401115234.png)
- $T'$ 的属性 $inh$ (inherit) 实际上**继承了**相应的 `*` 号的左预算分量。
![image.png|400](https://kold.oss-cn-shanghai.aliyuncs.com/20260401115342.png)
- 即我们把一个运算数($F.val=3$)作为继承属性，交给新引入的 $T'$ ,即 $T'.inh=3$。

### 消除直接左递归时的语义规则的处理

- 假设
	- $A\to A_{1}Y$, $A.a=g(A_{1}.a,Y.y)$
	- $A\to X,A.a=f(X,x)$ 
- 那么
	- $A\to XR,R.i=f(X.x);\;A.a=R.s$
	


