---
本科课程: ICS
author: 231275036-朱晗
date: 2025-12-13
completed: "true"
---

| stuid     | name   |
| --------- | ------ |
| 231275036 | 朱晗<br> |
|           |        |



作业清单
Chapter 7
- 3, 4,6
### 3
假定一个虚拟存储系统的虚拟地址为 40 位，物理地址为 36 位，页大小为 16KB。若页表中有效位、访问权限位、修改位、使用位，共占 4 位，磁盘地址不记录在页表中，则该存储系统中每个进程的页表大小为多少？如果按计算出来的实际大小构建页表，则会出现什么问题？

虚拟地址为 40 位，每个虚拟页大小为 `16KB = 14 bit`，故有 `26bit` 宽的 (`2^26`) 个虚拟页

物理页面和虚拟页面大小相同，所以有 `2^22` 个物理页

页表项 PTE: `valid + access bit + dirty bit + use bit + physical bit = 4 + 22 = 26`，由于对齐，我们使用 `32bit = 4B`

我们有 `2^26个` PTE
所以实际大小为 `2^26 * 4B = 2^28 B = 2^8 MB = 256 MB`

**每个进程对应一个页表**，一个页表如果就 256MB，就会出现操作系统很容易爆内存问题。总结一下：
	 **如果按实际大小构建**，会出现页表过大，无法装入内存的问题。

### 4 假定一个计算机系统中有一个 TLB 和一个 L1 Data Cache。该系统按字节编址，虚拟地址 16 位，物理地址 12 位，页大小为 128B； TLB 采取 4 路组相联方式，共有 16 个页表项；L1 Data Cache 采用直接映射方式，块大小为 4B，共 16 行。在系统运行到某一时刻时，TLB、页表和 L1 Data Cache 中的部分内容如图所示

1. 虚拟地址中哪几位表示虚拟页号？哪几位表示页内偏移量？虚拟页号中哪几位表示 TLB 标记？哪几位表示 TLB 组索引
虚拟地址 `16` 位，页大小为 `128B`, `128 = 2^7`,  故 7 bit 代表页 offset，高位的 9bit **代表虚拟页号**. 16 个页表项，4 路相联，故一共 4 组，需要 2 位组索引。
`15-7`: **虚拟页号**
- `15-9`: TLB tag
- `8-7`: TLB group index

`6-0`: 页内偏移


2. 物理地址中哪几位代表物理页号（**页框号**）？哪几位代表页内偏移量？
物理地址 12 bit, offset 和虚拟地址一致，故高 5 位代表物理页号(页框号), 低 7 位代表页内偏移量

3. 物理地址如何划分成标记字段、行索引字段和块内地址字段？
物理地址的划分机制是*相似的*
**行索引**：L1 Data Cache 直接映射，块大小为 4B，共 16 行，故需要 4bit 的行索引，块内偏移 `2bit`

|**PA 划分 (Cache)**|**PA[11:6]**|**PA[5:2]**|**PA[1:0]**|
|---|---|---|---|
|名称|Cache 标记 (Tag)|行索引 (Index)|**块内偏移量**|
|作用|确定**哪个块**|确定**哪一行**|确定块内**哪个字节**|

4. 若从虚拟地址 `0x067A` 中读取一个 `short` 型变量，则这个变量的值是多少？说明 CPU 读取虚拟地址 `067A` 内容的过程。

`0x067A`:
`0000 0110 0111 1010`
`0000 011`：tag：`03`
`00`： 第 `0` 组
- 可以看出访问的是第 `0` 组的 `03tag`，发现有效位为 `0`，**无效**
- TLB 无效，**继续访问主存中的页表**

高 9bit **虚拟页号为** `0C` ，查询到页框为 `19`, 则知道物理地址是多少了
物理地址的物理页号（前 5 位）页框，`0001 1001`，即 `11001`
**页偏移一致**
`1100 1111 1010`


tag： `11 0011`： `0x33`,
`line`: `1110`: `0xE`，发现 tag 和物理地址算出来的 tag 一致，且有效

**偏移量**： `0x10`: 说明是第 2 个字节，读取 `0x4A`

`short` 需要 2 个字节，~~所以我们读取的是 `0x4A2D`~~, **如果是小端机器**，则读出的是 0x2D4A


## 6
假设在 IA-32 + Linux 平台上运行一个 C 语言源程序 P 对应的用户进程，P 中有一条循环语句 S 如下
```c
for (i=0; i<N; i++) sum+=a[i];
```

- `sum` long
- `a` long
- 链接后，确定 `a` 的首地址为 `0x8049300`
- 假设首地址-> EDX, 下标变量 i -> ECX, sum -> EAX, 赋值语句 `sum+=a[i]` 仅用一条指令 I 实现，指令 I 的地址为 `0x8048c08`
- 已知 IA-32 + Linux 平台采用图所示的两级页表分页虚拟存储管理方式，页大小为 4KB，系统启动后控制寄存器 CR0 中的控制位 NW 和 CD 均为 0。假定系统中没有其他用户进程，该进程的页目录表首地址为 `0x3d000`，指令 I 对应页表项所在页表的首地址为 `0x5c8000` 指令 I 所在页在主存中分配的页框号为 `1020`，回答下列问题


### 指令序列
> [!Question] 1
> 假定常数 `N` 在 EBX 中，手工写出循环语句 S 对应的指令序列，与 GCC 生成的目标代码进行比较

```asm
.L_LOOP_START:
	cmpl	%ebx	%ecx 	# compare i and N: N - i > 0?
	jge		.L_LOOP_OUT
	I指令
	jmp .L_LOOP_START	# loop again.
.L_LOOP_OUT:
	# other instr
```




```
00001250 <loop_sum>:
    1250:	53                   	push   %ebx
    1251:	e8 ca fe ff ff       	call   1120 <__x86.get_pc_thunk.bx>
    1256:	81 c3 82 2d 00 00    	add    $0x2d82,%ebx
    125c:	8d 83 88 00 00 00    	lea    0x88(%ebx),%eax
    1262:	8b 93 68 00 00 00    	mov    0x68(%ebx),%edx
    1268:	8d 88 a0 0f 00 00    	lea    0xfa0(%eax),%ecx
    126e:	66 90                	xchg   %ax,%ax
    1270:	03 10                	add    (%eax),%edx
    1272:	83 c0 04             	add    $0x4,%eax
    1275:	39 c8                	cmp    %ecx,%eax
    1277:	75 f7                	jne    1270 <loop_sum+0x20>
    1279:	89 93 68 00 00 00    	mov    %edx,0x68(%ebx)
    127f:	5b                   	pop    %ebx
    1280:	c3                   	ret
```
这是在 `Linux-IA32` 编译下的结果。他使用 `call` 直接获取我定义的全局变量，并且直接使用数组地址来访存
```
1270:	03 10                	add    (%eax),%edx
1272:	83 c0 04             	add    $0x4,%eax
```
1270 是获取地址，加到 sum 中。然后直接把地址往后推 `0x4` 四个字节


---

### CR0 控制位
> [!Question] 执行到程序 P 时，控制寄存器 CR0 中的控制位 PE 和 PG 各是什么？

题目明确启动分页机制，故 PG (Paging Enable) =1
保护位默认开启，`PE=1` (保护模式)


### 指令 I 对应的汇编形式（AT&T）是什么？指令 I 中存储器操作数的寻址方式是什么？

```asm
addl (%edx, %ecx, 4) %eax
```
- 比例因子是 4，因为 32 机器每个数组元素 long 的大小为 *4 个字节*

基址+比例变址


### 执行指令
> [!Question] 指令行为理解
> 若指令指令 I 时，CS 段寄存器对应的描述符 cache 中存放的是表所示的用户代码段信息，且 CPL=3，DS 段寄存器对应的描述符 cache 存放的是用户数据段信息，
> - 则当 `i=50` 时，取指令操作过程中 MMU 得到的指令的线性地址是多少？
> - 取数操作中 MMU 得到的操作数的线性地址是多少？
> - 指令 I 所在页的虚页号是多少？
> - 指令 I 的线性地址中，页目录索引、页表索引和页内偏移量分别是什么？
> - 指令 I 对应的页目录项和页表项分别在主存中的哪个单元？指令 I 在主存中的哪个单元？第一次执行指令 I 时，指令 I 对应页表项中，字段 P、R/W、U/S、A 和 D 的内容各是什么？

回顾：

|**字段名称**|**英文缩写**|**线性地址位数**|**作用**|
|---|---|---|---|
|**页目录索引**|PDI|VA[31:22] (10 位)|用于查找页目录表 (Page Directory) 中的**页目录项 (PDE)**。|
|**页表索引**|PTI|VA[21:12] (10 位)|用于查找页表 (Page Table) 中的**页表项 (PTE)**。|
|**页内偏移量**|Offset|VA[11:0] (12 位)|用于定位页框 (Page Frame) 内的**具体字节**。|


- `i=50`, 取指令的线性地址为 `0x8048c08`
- 操作数的线性地址：`0x8049300 + 50 * sizeof(long) = 0x8049300 + 50 * 0x8 = 0x8049300 + 0x190 = 0x8049490` 
- 指令 I 所在的虚页号：
	- 页大小 4KB，页内偏移 `log(4KB)=2+10=12bit` 
	- 虚页号为剩下的 20 位 (high bit)
	- `0x08048`
- I 的
	- **确保计算步骤：**
		1. 先确定 **Offset** 的 12 位 ($\text{0xc08}$)。
		2. 对剩下的 **$\text{0x8048}$** 部分（PDI+PTI）进行分解。
		3. $\text{PDI}$ 是 $\text{0x8048} \gg 10 = 32$ ($\text{0x20}$).
		4. $\text{PTI}$ 是 $\text{0x8048} \pmod{1024} = 8$ ($\text{0x8}$).
	- **页目录索引**：高 10 位（因为要平均分）
		- `0b00 0010 0000`
		- `0x20`
	- 页表索引
		- 中间 10bit
		- `0b00 0100 1000`
		- `0x48`
		- **页表索引**：`0x8048(mod 0x400) = 0x48`
	- 页内偏移量：
		- `0xc08`
- I 的
	- 页目录项：
		- **之前计算**了页目录索引为 `0x20`, 每个页目录项（PDE） `4字节`，故
		- `0x3d000 + 4*0x20 = 0x3d080`
	- 页表项：
		- 计算了页表索引为 `0x48`
		- `0x5c8000 + 0x48 *4  = 0x5c8000 + 0x120 = 0x5c8120` 
	 - 指令 I 在主存的哪个单元？
		 - `1020 = 0x3fc`
		 - 页内偏移：`0xc08`
		- 则物理地址为 `0x3fcc08`
- 第一次执行指令 I 时，指令 I 对应页表项中，字段 P、R/W、U/S、A 和 D 的内容各是什么？
	-  `P: Present`
		- `1`, 已经在页中来
	-  `R/W` (Read-Write)
		- `1`，代码段需要允许操作
	- `U/S` (User/Supervisor)
		- `1` 用户程序
	- `A` (Access bit)
		- `1`
	- `D` (Dirty bit)
		- 第一次执行，还未被修改过，`0`

### 指令 I 在第一次执行过程中，有没有可能发生缺页异常？为什么？如果发生缺页异常的话，则页故障线性地址是什么？该地址会保存在哪个控制寄存器中？

第一次执行可能发生缺页异常
我们知道 I 的地址对应的页是 1020 页框，且 I 的 offset 为 `0xc08`, 页的大小为 4KB
`4KB = 0x1000`
则 `0x8048000 - 0x8049000` **这个范围的虚拟地址对应的页框**，已经被**加载到了主存**。但是我们访问的 `a[i]` 首地址为 `0x8049300`，不在这个框内，所以可能缺页。

如果缺页的话，则一定发生在访问第一个元素即执行过程，页故障线性地址是 `a[0]` 的地址 `0x8049300` 

会保存在 CR2 寄存器（线性地址故障寄存器）[[Lessons/ICS/ics - section 7 虚拟存储器#控制寄存器 CR|ics - section 7 虚拟存储器]]


### 指令 I 执行 TLB 
> [!Question] 指令 I 的执行 TLB 分析
> 指令 I 在第一次执行过程中，有没有可能发生 TLB 缺失？为什么？
> 若指令 TLB 共有 16 个表项，采取 4-wayset-associative，则虚拟页号中哪几位为 TLB tag？哪几位为 TLB index？
> 若第一次执行到指令 I 时，指令 TLB 的部分内容如下表（TLB 表中部分字缺失，in hex）, 则指令 I 所存放的主存地址是什么？

![image.png|400](https://kold.oss-cn-shanghai.aliyuncs.com/20251213141148.png)

- 第一次一定 TLB miss。因为 TLB 是一块 cache，缓存了 `VPN(Virtual Page Number) -> PFN(Physical Frame Number)` 的映射。第一次访问还没有缓存这个映射，故一定 TLB miss

- 16 entry, 4-wayset, then it has `16/4=4` sets
	- it has 4 sets, so we need `2` bit index to divide sets apart
	- 12 bit offset `32 - 2 -12 = 18`, 
	- then high 18 bit is ***tag***
	
第一次执行到指令 I，I 只能靠物理地址确定（TLB 事实上也没有存储 `0x3fc` 页框！）。
地址：`0x3fcc08`

## 7指令 cache
> [!Question] 指令 Cache 理解
> 若指令 cache 的数据区容量为 8KB，主存块大小为 32B，采取 2-wayset-associative，则指令 I 在第一次执行的取指令过程中会不会发生 cache 缺失？指令 I 所在的主存块应映射到指令 cache 的哪一组中？

第一次执行一定 cahche miss

`I`: `0x3fcc08`

`cache` 的数据区容量为 `8KB`，主存块大小 `32B`，可以容纳 `8KB /32B = 256` 块。
2 way-set associative, 则 128 个 set，每组两个

每个主存块内部需要 `32B`, 即 `5` bit offset

组需要 `7` bit
cache index: 
$$\text{PA 的低 12 位是 } \text{0xc08} = \text{0b} \underbrace{110000001000}_{\text{Offset}}$$
- `set index`: `0b1100000`
- `block offset`: `0b01000`
则分到了第 `0b1100000 = 96` 组，block offset = 8

我们映射到 cache 的 `96` 组中


## 8数组 a 占用页面
> [!Question] 数组 a 的页面分析
> 当 *N* = 2000 时，数组 a 占用几个页面？每个页面的虚页号是什么？数组元素 `a[1200]` 在哪个页中？

`siz`
N=2000, 数组 `a` 从 `0x8049300` ，占到 `0x8049300 + N * sizeof(long)`
`0x804d180`
而每一页大小是 `0x1000`
则一共 `0xd - 0x9 +1 = 5`
一共五页。
每个页面的虚页号为：

`0x8049`
`0x804a`
`0x804b`
`0x804c`
`0x804d`

数组元素 `1200`, 其位于 `0x804b880`, 位于 `0x804b` 页

