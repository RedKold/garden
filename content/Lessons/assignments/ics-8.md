作业清单：
第八章作业：教材第八章后习题中的第3、4、5题

## 3
根据表给出的 4 个进程的开始时刻和结束时刻，指出每个进程对 `P1-P2, P1-P3, P1-P4, P2-P3, P3-P4` 中的两个进程是否并发运行


| process | start time | end time |
| ------- | ---------- | -------- |
| P1      | 1          | 7        |
| P2      | 4          | 6        |
| P3      | 3          | 8        |
| P4      | 2          | 5        |

发现，所有进程对都有重叠区间。
所以都是并发
## 4 

```c
1 unsigned short b[2500];
2 unsigned short k;
3 main(){
4 	b[1000] = 1023;
5	b[2500] = 2049%k;
6	b[10000] = 20000;
7	return 0;
8 }

// compile, link, line5, 6, 7 inst series:

1 	movw	$0x3ff,	0x80497d0	// b[1000] = 1023
2	movw	0x804a388,	%cx		// R[cx] = k
3	movw	$0x801,		%ax		// R[ax] = 2049
4 	xorw	%dx,		%dx		// R[dx] = 0
5	div		%cx					// R[dx] = 2049 % k
6	movw	%dx,	0x804a388	// b[2500] = 2049 % k
7	movw	$0x4e20	0x804de20	// b[10000] = 20000
```

系统采取分页虚拟存储管理方式，页大小为 4KB，第一行指令对应的虚拟地址为 `0x80482c0`, 运行 P 对应的进程时，系统中没有其他进程在运行。回答：
1.  上述 7 条指令的执行，是否可能在取指令时发生缺页故障？
2.  执行第 1 行，第 2 行，第 6 行和第 7 行指令时，在访问存储器操作数的过程中是否会发生页故障或其他问题？哪些指令中的问题是可恢复的？哪些指令中的问题是不可恢复的？分别画出第 1 行和第 7 行指令所发生故障的处理过程示意图
3. 执行第 5 条指令会发生什么故障？该故障能否恢复？

### 4-1
第一行指令虚拟地址为 `0x80482c0`, 页大小为 `4KB = 2^2 * 2^10 = 2^12`, 即地址中 `3Byte` 是页内偏移量。
故，我们的虚拟地址所在页号为 `0x8048`, 页地址范围：`0x8048000 -> 0x8048fff`
该 7 行指令连续存储，显然不会超出 `0x8048fff` 的范围。即它们在同一页

由于该函数已经执行到 line5 对应的指令，故其对应的虚拟页已经加载，故不可能发生缺页。

### 4-2
- 第 1 行
	- 可能发生缺页。`0x80497d0` 在另一页。
	- 这是可以恢复的
```
用户进程:	执行指令 -> 	发现缺页					从加载的新页中读取数据	-> 继续用户程序
						+中断处理，保存现场 		+恢复现场
系统进程：				异常处理	->	调入主存	-> 	结束系统调用
```
- 第 2 行
	- 可能发生缺页
	- **可恢复**
- 第 6 行
	- 不可能发生缺页。已经在第 2 行加载该虚拟页
	- 但是会发生数组越界, 覆盖了 k（因为作为静态变量，它们是连续存储的）
- 第 7 行
	- 会发生段错误：**数组越界**。
	- 不可恢复


```
用户进程:
    执行 movw 指令
        → 写内存地址 0x804de20
        → MMU 查页表 → 无效页表项（valid=0）
        → 触发 Page Fault 异常
        + 保存现场
        → 切换到内核态

操作系统（系统进程）:
        异常处理程序开始
        → 获取出错地址：0x804de20
        → 查询该地址是否属于进程的合法地址空间：
        → 判定为 非法地址访问
        → 向进程发送 SIGSEGV（Segmentation Fault）信号
结束
```

### 4-3
`k` 作为全局变量，在 `.bss` 中默认值是 0.
所以会发生除 0 错误，触发 `SIGFPE` 信号。
该故障无法恢复。
## 5 hello world
若用户程序希望将 `“hello world!\n”` 中的 14 个字符显示在 stdout 上，则可以使用系统调用 `write` 对应的封装函数 `write(1, "hello, world!\n",14)`，在 IA-32 + Linux 系统中，可以用以下 asm 代码实现

```asm
1	movl	$4,		%eax	// sysno=4, to EAX
2	movl	$1,		$ebx	// stdout fd =1, to EBX
3	movl	$string,%eax	// the address of "hello world!\n", to ECX
4 	movl	$14,	%edx	// len(str) = 14,	to EDX
5	int		$0x80			// syscall
```

回答：
1. 执行该段代码，系统处于用户态还是内核态？为什么？执行完第 5 行指令后的下一个时钟周期，系统处于用户态还是内核态？
2. 第 5 行指令是否属于陷阱指令？执行该指令时，通过五种类型门（中断门、系统门、系统中断门、陷阱门和任务门）描述符中的那种类型门描述符来激活异常处理程序？对应的中断类型号时多少？对应门描述符中的字段 P、DPL、TYPE 的内容分别是什么？根据对应门描述符中的段选择符取出的 GDT 中的段描述符中的基地址、限界、字段 G、S、TYPE（包含 A）、DPL、D 和 P 分别是什么？
3. 详细描述第 5 行指令的执行过程

### 5-1
用户态。
该段代码实际是从用户态到内核态转化的过程，`int` 指令使我们进入系统调用环节，但是这 5 条指令仍然在用户态
下一个时钟周期，我们 trigger 了 `trap` 将进入内核态

### 5-2
属于 trap
属于系统门
类型号 `0x80`
DPL=3
TYPE=1110B
P =1, 因为 linux 从来不会把段交换到外存中，而是以页为单位交换

取出 GDT 中的段描述符：
- 基地址
	- 线性地址 (0x0)
- 限界
	- 0xFFFFFF (4GB)
- 字段 G
	- 1
- S（系统可用）
	- 1
- TYPE（包含 A）
	- 1010B
	- A=1 (Access), 是最后一位
	- 可执行，非一致，可读，未访问
- DPL
	- 0 (System level)
- D
	- 1
- P
	- Present: 1

### 5-3

1. 开始执行`int $0x80`，完成用户态到内核态的转化
2. 读TR 寄存器，访问该运行进程的TSS 段（Task Stack Segment）
3. 将TSS 段中保存的内核栈的**段选择符**和栈指针分别装入寄存器SS 和ESP，然后在内核栈中保存原来用户栈的**SS 和ESP**
	**这一步是为了切换到内核栈执行程序**
	- **Segment Selector** SS: 选择哪个段
4. 根据中断号访问 `IDT[i]`, 将段选择符装入 CS，IDTi 的偏移地址装入 `EIP`，即中断处理函数（系统调用）的逻辑地址
	1. IDTi 存储了一个 `offset`，指示了我们去多少偏移量执行地址
	2. EIP:**Extended Instruction Pointer**，即PC 的地址。
	3. CS: *Code Segment Register*，段选择符装入这里是为了确定内核代码段。
5. 执行中断处理程序
6. `iret`：从内核栈弹出EIP、CS 和EFLAGS，恢复断点和程序状态（**复原现场**）
7. 回到原用户程序第五条的下一条继续执行


