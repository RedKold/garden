## Section 3 程序的转换和指令系统

- **高级语言** -> **汇编代码**
- **汇编指令** 是机器指令的**汇编表示形式**

- 全局变量 `int a = 100`
	- **直接就在全局变量数据区**
- 局部变量 `int a = 100`
	- **变量**在一条指令的 `imm` 立即数中。


### Look back: Storage of  Data
- 数据存放在哪里？
	- **通用寄存器 `GPRs`** 32 位机有 8 个寄存器。

- **一条指令的信息**
	- 操作码： 制定操作类型
		- **操作码长度**
	- **源操作数参照** 一个或多个源操作数所在的地址
		- （操作数来源： 主（虚）存/ 寄存器/ IO 端口）


### 高级语言程序转化为机器代码的过程

- **预处理** `hello.c`
	- 替换 `#include` 和 `#define` 宏
- **编译** `hello.i`
	- 预处理后的源程序文件编译生成相应的 **汇编语言程序**
- **汇编** `hello.s` 
	- **由汇编程序**将 **汇编语言源程序** 转换为可重定位的 **机器语言目标代码文件**
- **链接** `hello.o`
	- 由链接器将多个可重定位的机器语言目标文件以及库例程序（如 `printf()` 库函数）连接起来了形成最终的 **可执行目标程序**
- `hello`

`gcc -c test.s -o test.o` 汇编
`objdump -d hello`：反汇编


- 以下是我用 `Macbook Pro M4` 编译的结果。指令集 `ARM ASM`
```armasb
objdump -d hello

hello:	file format mach-o arm64

Disassembly of section __TEXT,__text:

0000000100000460 <_main>:
100000460: d10083ff    	sub	sp, sp, #0x20
100000464: a9017bfd    	stp	x29, x30, [sp, #0x10]
100000468: 910043fd    	add	x29, sp, #0x10
10000046c: 52800008    	mov	w8, #0x0                ; =0
100000470: b9000be8    	str	w8, [sp, #0x8]
100000474: b81fc3bf    	stur	wzr, [x29, #-0x4]
100000478: 90000000    	adrp	x0, 0x100000000 <_printf+0x100000000>
10000047c: 91128000    	add	x0, x0, #0x4a0
100000480: 94000005    	bl	0x100000494 <_printf+0x100000494>
100000484: b9400be0    	ldr	w0, [sp, #0x8]
100000488: a9417bfd    	ldp	x29, x30, [sp, #0x10]
10000048c: 910083ff    	add	sp, sp, #0x20
100000490: d65f03c0    	ret

Disassembly of section __TEXT,__stubs:

0000000100000494 <__stubs>:
100000494: 90000030    	adrp	x16, 0x100004000 <_printf+0x100004000>
100000498: f9400210    	ldr	x16, [x16]
10000049c: d61f0200    	br	x16
```


### 可执行文件的存储器映像

![image.png|600](https://kold.oss-cn-shanghai.aliyuncs.com/20250915111619.png)

- 程序（段）**头表的映射方式**


### 一个指令的地址字段
- **零地址指令**
	- 无需操作数
	- 所需操作数是默认的 （堆栈，累加器）
#### 零地址指令（Zero-Address Instructions）

- **工作原理**：指令中不包含任何操作数地址。所有操作数都隐含地从一个被称为**栈**（Stack）的数据结构中获取。
- **执行过程**：指令会从栈顶弹出（pop）一个或多个操作数进行运算，然后将结果压入（push）栈顶。
- **优点**：指令长度短，编码简单。
- **缺点**：需要额外的栈操作来准备操作数，程序代码可能较长。
- **例子**：`ADD`。这条指令会弹出栈顶的两个操作数，相加后将结果压回栈顶。这种设计常见于**栈式机**（Stack Machine），例如早期的虚拟机（JVM）和某些计算器。
---

#### 一地址指令（One-Address Instructions）

- **工作原理**：指令包含一个操作数地址。通常，另一个操作数隐含地存储在一个特殊的寄存器中，这个寄存器被称为**累加器**（Accumulator）。
- **执行过程**：指令会从指定的地址获取一个操作数，并将其与累加器中的值进行运算，然后将结果存回累加器。
- **优点**：指令相对简洁，比零地址指令更易于理解。
- **缺点**：所有操作都依赖于累加器，灵活性受限。
- **例子**：`ADD X`。这条指令表示将内存地址 X 处的数据与累加器中的值相加，并将结果存入累加器。这种设计在早期的计算机架构中很常见。
---

#### 二地址指令（Two-Address Instructions）

- **工作原理**：指令包含两个操作数地址。其中一个地址既是源地址，也是目的地址。
- **执行过程**：指令从两个地址获取操作数，运算后将结果存入第一个地址。
- **优点**：比一地址指令更灵活，指令更紧凑。
- **缺点**：源操作数之一会被覆盖，需要额外的指令来保存原始值（如果需要的话）。
- **例子**：`ADD R1, R2`。这条指令表示将寄存器 R1 和 R2 中的值相加，并将结果存回 R1。这种模式在大多数**精简指令集计算机（RISC）** 架构（如 ARM、MIPS）和**复杂指令集计算机（CISC）**架构（如 x86）中都非常普遍。

---

#### 三地址指令（Three-Address Instructions）
(`RISC` 风格)
- **工作原理**：指令包含三个操作数地址。两个地址作为源操作数，一个地址作为目的地址。
- **执行过程**：指令从两个源地址获取操作数，运算后将结果存入一个独立的目的地址。
- **优点**：指令明确，每个操作数都有独立的地址，不会覆盖源数据，易于编写和理解。
- **缺点**：指令长度较长，因为需要编码三个地址。
- **例子**：`ADD R3, R1, R2`。这条指令表示将寄存器 R1 和 R2 中的值相加，并将结果存入寄存器 R3。这种模式在现代 RISC 架构中很常见，因为它允许编译器生成更优化的代码。

### IA-32 定点寄存器组织
- 8 个通用寄存器 `GPRs`
	- 这些是 x86 架构中主要的 8 个 32 位通用寄存器，它们的 64 位版本通常以 `R` 开头（例如，EAX 对应 RAX），16 位版本以 `A` 开头（例如，EAX 对应 AX）。
	- **`EAX`**：**E**xtended **A**ccumulator Register（扩展累加器）。
	    - **历史用途**：主要用于算术运算的累加器。
	    - **现代用途**：在 C/C++ 等语言中，`EAX` 寄存器通常用于存放**函数调用的返回值**。
		
	- **`EBX`**：**E**xtended **B**ase Register（扩展基址寄存器）。
	    
	    - **历史用途**：通常用作内存地址的基址，用于访问数据。
	        
	    - **现代用途**：在 64 位模式下，`RBX` 通常用作**非易失性寄存器**，函数调用时需要由被调用函数保存和恢复其值。
	        
	- **`ECX`**：**E**xtended **C**ount Register（扩展计数寄存器）。
	    
	    - **历史用途**：主要用于循环和字符串操作中的计数器。
	        
	    - **现代用途**：在 `REP` 指令家族（如 `REP MOVSB`）中，`ECX` 寄存器仍然用作**循环计数器**。
	        
	- **`EDX`**：**E**xtended **D**ata Register（扩展数据寄存器）。
	    
	    - **历史用途**：在乘除法运算中，与 `EAX` 配合使用来存放 64 位结果或被除数的另一半。
	        
	    - **现代用途**：与 `EAX` 一起用于处理 64 位整数运算。在函数调用中，它有时也用于传递参数。
	        
	- **`ESP`**：**E**xtended **S**tack **P**ointer（扩展栈指针）。
	    
	    - **用途**：指向**栈顶**的地址。它负责跟踪栈的当前位置，所有 `push` 和 `pop` 操作都会隐式地修改它。`ESP` 在函数调用和返回时非常关键。
	        
	- **`EBP`**：**E**xtended **B**ase **P**ointer（扩展基址指针）。
	    
	    - **用途**：通常用于指向**栈帧的基址**。它提供了一个稳定的参考点，方便程序通过固定的偏移量来访问函数的局部变量和参数。
	        
	- **`ESI`**：**E**xtended **S**ource **I**ndex（扩展源变址寄存器）。
	    
	    - **用途**：在字符串和内存块操作中，通常用作**源数据**的地址指针。
	        
	- **`EDI`**：**E**xtended **D**estination **I**ndex（扩展目的变址寄存器）。
	    
	    - **用途**：在字符串和内存块操作中，通常用作**目的数据**的地址指针。
- 2 个 **专用寄存器**
	- `EIP`:  **E**xtended **I**nstruction **P**ointer
	- `EFLAGS`: 标志寄存器
	- ![image.png|400](https://kold.oss-cn-shanghai.aliyuncs.com/20251015005141.png)
		- ZF：零标志位
			- 为 `0` 则 `1`
		- PF：奇偶 (Parity)
			- 偶数个：`1`
		- SF：符号
			- 负数则 `1`
		- CF：进位/借位
			- 进位则 `1` 
			- 借位则 `1`（不够减）
			- 对于带符号的，**没有意义**
		- OF：溢出（带符号的发生溢出）
			- 对于无符号的，**没有意义**
[[DLCO-0 数字逻辑与计算机组成#n 位整数加/减运算器]]


- 6 个 **段寄存器**
	- `CS`： 代码段
	- `SS`： 栈段
	- `DS`： 数据段
	- `ES`： 附加段
	- `FS`： 附加段
	- `GS`： 附加段

---
**TIPS**
- 你只能使用通用寄存器，做寻址。不要用其他专用寄存器！
### x86-64
相比于 `IA-32` 的进步
- **通用寄存器（GPRs）**：从 IA-32 的 8 个（EAX, EBX, ECX, EDX, ESI, EDI, ESP, EBP）增加到 x86-64 的 **16 个**（RAX, RBX, RCX, RDX, RSI, RDI, RSP, RBP, R8-R15）。
- **寄存器位宽**：所有通用寄存器从 32 位扩展到 **64 位**。

- 向上兼容


### 寻址方式 Addressing Model
- **指令或操作数地址的指定方式**
	- 根据地址找到指令或操作数的方法

- **寻址方式的确定**
	- **没有专门的寻址方式**位 （由操作码确定）
		- `MIPS`
	- **有专门的寻址方式位**
		- `x86`
- **有效地址**
	- 操作数所在的存储单元的地址（可能是逻辑地址或物理地址），**可通过指令的寻址方式和地址码计算得到**
- **基本寻址方式**
	- 立即（立即数）
		- `$0xFFF000`
	- 直接 （读地址）
		- **指令中给出的地址码是操作数的有效地址**
		- **需要用括号括起来，否则无法解读**（从汇编来说）
	- 间接 （`*`）
		- 指令中给出的地址码是存放操作数有效地址的 **存储单元** 的地址。
		- 跟着再读一次（AT&T: `(%ebx)`）
	- 寄存器
		- 指令中给出的地址码是操作数所在的寄存器编号
		- **寄存器直接寻址**
	- 寄存器间接
		- 指令中的地址码给出了一个寄存器编号，**这个寄存器存储了** 操作数的有效地址
	- 偏移
	- 栈

### AT&T 汇编指令
**操作数格式：**

|                |                        |            |                                  |
| -------------- | ---------------------- | ---------- | -------------------------------- |
| 格式             | 操作数值                   | 名称         | 样例（GAS = C语言）                    |
| `$Imm`         | `Imm`                  | 立即数寻址      | `$1 = 1`                         |
| `Ea`           | `R[Ea]`                | 寄存器寻址      | `%eax = eax`                     |
| `Imm `         | `M[Imm]`               | 绝对寻址       | `0x104 = *0x104 `                |
| `（Ea）`         | `M[R[Ea]]`             | 间接寻址       | `（%eax）= *eax`                   |
| `Imm(Ea)`      | `M[Imm+R[Ea]]`         | (基址+偏移量)寻址 | `4(%eax) = *(4+eax)`             |
| `（Ea,Eb）`      | `M[R[Ea]+R[Eb]]`       | 变址寻址       | `(%eax,%ebx) = *(eax+ebx`)`      |
| `Imm（Ea,Eb）`   | `M[Imm+R[Ea]+R[Eb]]`   | 变址寻址+偏移量   | `9(%eax,%ebx)= *(9+eax+ebx)`     |
| `(,Ea,s)`<br>  | `M[R[Ea]*s]`           | 伸缩化变址寻址    | `(,%eax,4)= *(eax*4)`            |
| `Imm(,Ea,s)`   | `M[Imm+R[Ea]*s]`       | 伸缩化变址寻址    | `0xfc(,%eax,4)= *(0xfc+eax*4)`   |
| `(Ea,Eb,s)`    | `M(R[Ea]+R[Eb]*s) `    | 伸缩化变址寻址    | `(%eax,%ebx,4) = *(eax+ebx*4)`   |
| `Imm(Ea,Eb,s)` | `M(Imm+R[Ea]+R[Eb]*s)` | 伸缩化变址寻址    | `8(%eax,%ebx,4) = *(8+eax+ebx*4` |


|后缀|操作数长度|适用寄存器示例|
|---|---|---|
|`b`|8 位 (byte)|`%al`, `%ah`, `%dl`|
|`w`|16 位 (word)|`%ax`, `%bx`|
|`l`|32 位 (long)|`%eax`, `%ebx`|
|`q`|64 位 (quad)|`%rax`, `%rcx`|
- `b, byte`
- `w, word`
- `l, long`
- `q, quad`

- ****
![image.png|400](https://kold.oss-cn-shanghai.aliyuncs.com/20251108125345.png)

### Intel x86 机器指令格式


`Intel x86` 增加了一些前缀，来解析操作器的位数、有效地址计算方式等信息，这点和 `RISC-V` 固定的寄存器字段有很大区别。

![image.png|400](https://kold.oss-cn-shanghai.aliyuncs.com/20250922105027.png)
- `SIB` ： 有的指令，除了 `MODR/M` 字节还需要一个 `SIB` 字节补充操作数的寻址方式，`0/1` 个字节（由 `ModR/M` 中的 `mod` 域和 `R/M` 域决定是否需要）
	- `SIB`: Scale Index Base
	
```
Bit:  7   6   5   4   3   2   1   0
     +---+-------+---------------+
     |SS | INDEX |     BASE      |
     +---+-------+---------------+
```

### 指令的不同形式

- ![image.png|500](https://kold.oss-cn-shanghai.aliyuncs.com/20250922104133.png)
- 一个例子

```
1000fe: 66 c7 84 99 00 e0 ff ff 01 00
```

---

#### 1. 指令前缀

```
66
```

- **操作数大小覆盖前缀 (Operand-size override prefix)**。  
    在 32 位模式下：让 `mov imm32` 变成 `mov imm16`。  
    在 64 位模式下：让 `mov imm32` 变成 `mov imm16`。
    

所以这里最终是 **往内存里写 16 位数**。

---

#### 2. 主操作码

```
c7
```

- **`C7 /0` = MOV r/m16, imm16 或 MOV r/m32, imm32**  
    （因为有 66h 前缀，所以是 `mov r/m16, imm16`）。
    

---

#### 3. ModR/M 字节

```
84
```

二进制：`1000 0100`

- Mod = `10` (内存地址 = 寄存器 + disp32)
    
- Reg = `000` (对应 `MOV` 的扩展码 `/0`)
    
- R/M = `100` → 特殊：表示 SIB 字节跟随
    

---

#### 4. SIB 字节

```
99
```

二进制：`1001 1001`

- Scale = `10` (缩放因子 = 4)
    
- Index = `011` (寄存器 EBX)
    
- Base = `001` (寄存器 ECX)
    

所以地址形式 = `ECX + EBX*4 + disp32`。

---

#### 5. displacement (位移)

```
00 e0 ff ff
```

小端序 → `0xFFFFE000` (有符号数，= -0x2000)。

---

#### 6. immediate (立即数)

```
01 00
```

小端序 → `0x0001` (16 位，因为有 66 前缀)。

---

#### 7. 汇编指令最终形式

```
mov WORD PTR [ecx + ebx*4 - 0x2000], 0x1
```

---

✅ **总结**  
这条指令的意思是：  
把 **常数 1 (16 位)** 存入内存单元 `[ecx + ebx*4 - 0x2000]`。

### 常用指令 `x86`

#### 比较大小类 `cmp`
```
cmpl %ebx, %eax     # 计算 EAX - EBX
jle  label          # 如果 EAX <= EBX (有符号比较)，则跳转
jb   label          # 如果 EAX < EBX (无符号比较)，则跳转
je   label          # 如果 EAX = EBX，则跳转
```

`jle: jump if less or equal `
j
`jb: jump if below （无符号）`
`ja: jump if Above （无符号）`

#### 四则运算

好的，以下是 x86/x86-64 汇编中常用的四则运算指令，根据有符号和无符号操作进行的总结表格。所有指令后缀（如 `b`, `w`, `l`, `q`）取决于操作数的大小（8位、16位、32位、64位）。

| **运算类型** | **算术运算**                   | **有符号 (Signed) 指令**       | **无符号 (Unsigned) 指令** | **结果存储和特点**                                                                                                                            |
| -------- | -------------------------- | ------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| **加法**   | $\text{A} + \text{B}$      | `ADD`                     | `ADD`                 | 结果存储在目标操作数中。有符号溢出由 $\text{OF}$ 标志指示；无符号进位由 $\text{CF}$ 标志指示。                                                                           |
| **减法**   | $\text{A} - \text{B}$      | `SUB`                     | `SUB`                 | 结果存储在目标操作数中。有符号溢出由 $\text{OF}$ 标志指示；无符号借位由 $\text{CF}$ 标志指示。                                                                           |
| **乘法**   | $\text{A} \times \text{B}$ | `IMUL` (Integer Multiply) | `MUL` (Multiply)      | **`MUL`** 和 **`IMUL` (单操作数)** 结果为操作数宽度的**两倍**（例如 $32 \times 32 \to 64$ 位 $\text{EDX}:\text{EAX}$）。 **`IMUL` (双/三操作数)** 结果为操作数**单倍宽度**。 |
| **除法**   | $\text{A} / \text{B}$      | `IDIV` (Integer Divide)   | `DIV` (Divide)        | **被除数**必须预先进行**符号扩展**（`IDIV` 使用 `cwd`/`cdq`/`cqo`）或**零扩展**（`DIV` 清空高位寄存器）。 **商**存储在 $\text{A}$ 寄存器中，**余数**存储在 $\text{D}$ 寄存器中。         |

##### 关键点简述：

1. **加减法 (`ADD`, `SUB`)：** 这两条指令本身不区分有符号和无符号。**区别在于它们如何设置标志位**，以及程序员如何使用这些标志位进行条件跳转（例如，`JO/JNO` 用于有符号溢出，`JC/JNC` 用于无符号进位/借位）。
    
2. **乘法 (`IMUL`, `MUL`)：** 这是两条不同的指令，直接在硬件上区分了有符号和无符号乘法。
    
3. **除法 (`IDIV`, `DIV`)：** 这是两条不同的指令，直接在硬件上区分了有符号和无符号除法，并且对被除数的准备方式不同。
    

##### 寄存器使用总结（以 32 位操作为例，后缀为 `l`）：

| **指令**          | **隐含的被乘/被除数**                     | **隐含的结果/商**           | **隐含的余数/高位**          |
| --------------- | --------------------------------- | --------------------- | --------------------- |
| **`mull src`**  | $\text{EAX}$                      | $\text{EAX}$ (低 32 位) | $\text{EDX}$ (高 32 位) |
| **`imull src`** | $\text{EAX}$                      | $\text{EAX}$ (低 32 位) | $\text{EDX}$ (高 32 位) |
| **`divl src`**  | $\text{EDX}:\text{EAX}$ (必须是无符号)  | $\text{EAX}$ (商)      | $\text{EDX}$ (余数)     |
| **`idivl src`** | $\text{EDX}:\text{EAX}$ (必须是符号扩展) | $\text{EAX}$ (商)      | $\text{EDX}$ (余数)     |

---